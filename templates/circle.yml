machine:
  timezone:
    America/New_York
  node:
    version: 6.2.0
  environment:
    NODE_ENV: production

dependencies:
  cache_directories:
    - node_modules
  override:
    - npm install --dev

deployment:
  production:
    tag: /v.*/
    commands:
      - npm run deploy:production
  staging:
    branch: master
    commands:
      - npm run deploy
